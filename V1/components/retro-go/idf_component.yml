## IDF Component Manager Configuration for retro-go shared component.
## See: https://docs.espressif.com/projects/idf-component-manager/en/latest/

dependencies:
  espressif/mdns:
    version: "^1.2.0"
    rules:
      - if: "idf_version >=5.0"
  espressif/usb_host_uac:
    version: "~1.2.0"
    rules:
      - if: "idf_version >=5.0"
